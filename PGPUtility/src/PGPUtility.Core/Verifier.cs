namespace PGPUtility.Core
{
    public class Verifier
    {
        // Methods:
        // VerifyString(string message, string signaturePath, string publicKeyPath)
        // VerifyFile(string filePath, string signaturePath, string publicKeyPath)
    }
}